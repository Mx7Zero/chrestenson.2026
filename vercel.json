{
  "buildCommand": "cd fix && npm install && npm run build",
  "outputDirectory": "fix/dist",
  "devCommand": "cd fix && npm run dev",
  "installCommand": "cd fix && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
